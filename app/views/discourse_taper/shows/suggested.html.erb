<%- @page_title = t("taper.suggest.thanks_title") %>
<div class="taper-suggest taper-suggest--done">
  <h1><%= t("taper.suggest.thanks_title") %></h1>
  <p><%= t("taper.suggest.thanks_body") %></p>
  <p>
    <a class="btn btn-primary" href="<%= @back_url %>"><%= t("taper.suggest.back") %></a>
  </p>
</div>
